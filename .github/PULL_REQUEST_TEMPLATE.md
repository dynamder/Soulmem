## 变更描述

<!-- 请简要描述本次 PR 做了什么，为什么这样做。 -->

## 测试

- [ ] `cargo build --workspace --all-targets` 通过
- [ ] `cargo test --workspace` 通过
- [ ] `cargo fmt --all --check` 通过
- [ ] `cargo clippy --workspace --all-targets -- -D warnings` 通过
- [ ] `cargo mutants --workspace` 杀灭率 ≥90%（CI 会按改动范围自动检查）

<!-- 如果本次改动涉及需要下载模型或真实 LLM 的测试，请说明它们如何被处理（如 #[ignore]）。 -->

## 关联 Issue

<!-- 如有关联 Issue 请填写，例如 Closes #123 -->
