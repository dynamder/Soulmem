# Flutter 构建产物
build/
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies

# 生成的 FRB 绑定**入库**（flutter_rust_bridge 惯例）：clone 后无需 codegen 即可编译；
# 改 api.rs 后需重新生成（`flutter_rust_bridge_codegen generate`），生成 diff 随代码提交

# IDE
.idea/
*.iml
.vscode/

# 其他
*.log
