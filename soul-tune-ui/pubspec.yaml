name: soul_tune_ui
description: Soul-Tune 测试框架 GUI（Flutter + flutter_rust_bridge，纯声明式渲染）
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.13.0

dependencies:
  flutter:
    sdk: flutter
  flutter_rust_bridge: 2.12.0
  file_picker: ^8.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
