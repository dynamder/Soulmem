# Soul-Tune UI 设计

原 ratatui TUI 的 UI 设计文档已随 TUI 移除（GUI 达 parity 后清理）。

GUI 交互设计见 [`soul-tune-ui/UI_DESIGN.md`](../../soul-tune-ui/UI_DESIGN.md)：
信息架构沿用 TUI（指标分组 → 逐用例状态 → 钻取），交互全面 GUI 化
（卡片入口 / 表单 / 原生文件对话框 / 可排序表格 / 点击钻取），柔和低对比度状态色。
